name: Publish PowerShell Module
on: [push]
jobs:
  publish-to-gallery:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and publish        
        shell: pwsh
        run: |
          echo "Hello World"
