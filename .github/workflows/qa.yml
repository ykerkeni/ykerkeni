name: Publish PowerShell Module
on: [push]
jobs:
  jenkins-trigger-push:    
    runs-on: windows-latest
    steps:
    - name: Trigger jenkins job
      uses: ykerkeni/HamdiMarwa/jenkins-trigger-windows/jenkins-trigger@main     
